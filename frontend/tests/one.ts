import 'mocha'
import { expect } from 'chai'

describe('Client Component', () => {
  xit('should render clients correctly', async () => {})
  xit('should render all inputs needed for new client creation', async () => {})
  xit('should callback to createClient prop when correctly filled out', async () => {})
  xit('should not callback to createClient prop when incorrectly filled out', async () => {})
})