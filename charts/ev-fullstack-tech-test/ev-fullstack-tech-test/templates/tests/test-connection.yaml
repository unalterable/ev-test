apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ev-fullstack-tech-test.fullname" . }}-test-connection"
  labels:
    {{- include "ev-fullstack-tech-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ev-fullstack-tech-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
