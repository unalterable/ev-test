{
  "name": "ev-fullstack-tech-test",
  "version": "1.0.0",
  "private": true,
  "author": "EV Dev <dev@ev.uk>",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "fe": "yarn workspace frontend start",
    "be": "yarn workspace backend start",
    "fe:test": "yarn workspace frontend run test",
    "test": "yarn workspaces foreach -pt run test"
  },
  "devDependencies": {
    "jest": "^27.1.0"
  }
}
